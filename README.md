# 🛡️ Security SOC Scan - Telegram AI Security & VirusTotal Scanner Bot

**Security SOC Scan** គឺជាប្រព័ន្ធការពារ និងផ្ទៀងផ្ទាត់សុវត្ថិភាពសារ Telegram ស្វ័យប្រវត្តិ ដោយប្រើប្រាស់ **AI Threat Content Verification Engine** និង **VirusTotal v3 REST API**។

---

## 🌟 លក្ខណៈពិសេសចម្បង (Key Features)

1. **វិភាគផ្ទៀងផ្ទាត់ខ្លឹមសារសារដោយ AI (AI Message Verification):**
   - ស្គាល់យ៉ាងឆ្លាតវៃនូវ **ភាសាគំរាមកំហែងបង្កភ័យ**, **ការក្លែងបន្លំជាធនាគារ (ABA, ACLEDA, Wing, etc.)**, **ការបង្ខំឱ្យផ្ទេរប្រាក់**, **សុំលេខកូដ OTP** និង **ល្បិចបោកប្រាស់ទូទៅ** ទាំងភាសាខ្មែរនិងភាសាអង់គ្លេស។

2. **ស្កេនតំណភ្ជាប់ & ឯកសារគ្រោះថ្នាក់ (VirusTotal v3 API):**
   - ពិនិត្យមើល URL និង ឯកសារគ្រប់ប្រភេទដូចជា `.APK`, `.EXE`, `.ZIP`, `.7Z`, `.Z`, `.RAR`, `.TAR`, `.GZ`, `.ISO`, `.PDF`, `.MSI` ដែលជនខិលខូចនិយមប្រើដើម្បីបោកប្រាស់ ឬបង្កប់មេរោគ (Malware / Ransomware) លើ Telegram។

3. **ប្រព័ន្ធការពារសម្រាប់ក្រុមពិភាក្សា (Group Chat Protection):**
   - គ្រាន់តែទាញ Bot ចូលទៅក្នុង Group Chat សមាជិកទាំងអស់នឹងមានកន្លែងសុវត្ថិភាពមួយសម្រាប់ផ្ទៀងផ្ទាត់រាល់មាតិកាសង្ស័យ មុននឹងសម្រេចចិត្តចុចបើក។

4. **Web Security SOC Dashboard (Ready for Vercel & Supabase):**
   - ផ្ទាំងគ្រប់គ្រង SOC Dashboard ទំនើប (Dark Mode, Glassmorphism design) បង្ហាញពីប្រវត្តិនៃការស្កេន ទិន្នន័យ Telemetry និង ឧបករណ៍ធ្វើតេស្តស្កេន (AI Threat Simulator)។

---

## 🐙 របៀប Push ទៅកាន់ GitHub (How to Push to GitHub)

1. **បង្កើត Repository ថ្មីនៅលើ GitHub** (ឧទាហរណ៍៖ `Security_SOC_SCAN`)
2. **តភ្ជាប់ និង Push កូដ៖**
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/Security_SOC_SCAN.git
   git branch -M main
   git push -u origin main
   ```

---

## ☁️ របៀប Deploy លើ Vercel & Supabase

### 1️⃣ Supabase Setup (Cloud Database)
1. បង្កើត Project ថ្មីនៅលើ [Supabase](https://supabase.com)។
2. បង្កើត Table `scan_logs` ក្នុង SQL Editor៖
   ```sql
   CREATE TABLE scan_logs (
       id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
       created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
       chat_id TEXT,
       chat_type TEXT,
       sender_id TEXT,
       scan_type TEXT,
       input_summary TEXT,
       risk_level TEXT,
       risk_score INT,
       threat_details JSONB,
       virustotal_details JSONB
   );
   ```
3. ចម្លង `Project URL` និង `anon public key` យកទៅដាក់ក្នុង Environment Variables។

### 2️⃣ Vercel Setup (Serverless Web App)
1. ចូលទៅកាន់ [Vercel](https://vercel.com) ➔ Import គម្រោងពី GitHub (`Security_SOC_SCAN`)។
2. បញ្ចូល **Environment Variables** ក្នុង Vercel Settings:
   - `TELEGRAM_BOT_TOKEN`: Token របស់អ្នកពី BotFather
   - `VIRUSTOTAL_API_KEY`: API Key របស់អ្នកពី VirusTotal
   - `SUPABASE_URL`: URL របស់ Supabase
   - `SUPABASE_KEY`: Public Anon Key របស់ Supabase
3. ចុច **Deploy**! Vercel នឹង Deploy Web SOC Dashboard របស់អ្នកជាលក្ខណៈ Serverless ភ្លាមៗ!

---

## 📁 រចនាសម្ព័ន្ធគម្រោង (Project Architecture)

```
Security_SOC_SCAN/
├── bot.py             # Telegram Security Bot (DM & Group Protection)
├── server.py          # FastAPI Server & Web SOC Dashboard API (Vercel Ready)
├── ai_analyzer.py     # AI Threat & Scam Engine (Khmer & English)
├── vt_scanner.py      # VirusTotal v3 REST API Scanner
├── database.py        # Dual Database Driver (Supabase Cloud + Local SQLite)
├── config.py          # Global Config & Constants
├── vercel.json        # Vercel Serverless Build Spec
├── requirements.txt   # Python Dependencies
├── .gitignore         # Git Ignore Template
├── static/            # Web SOC Dashboard UI
│   ├── index.html
│   ├── css/styles.css
│   └── js/app.js
└── README.md          # Project Documentation
```
